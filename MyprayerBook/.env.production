# Vercel deployment URL — fill in after first deploy (vercel --prod)
# Example: VITE_API_BASE_URL=https://myprayerbook.vercel.app
# For the web app on Vercel itself, this can be left empty (same-origin).
# For the Android Capacitor build, set it to your full Vercel URL.
VITE_API_BASE_URL=
