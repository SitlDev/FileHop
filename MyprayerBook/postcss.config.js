// PostCSS config — intentionally minimal.
// This file exists to prevent Vite from searching parent directories
// for a PostCSS config (which causes EPERM on /Users/amn/package.json).
export default {};
