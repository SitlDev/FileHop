[phases.setup]
nixPkgs = ["python311", "nodejs-20_x"]

[phases.build]
cmds = ["npm install --prefix webapp", "npm run build --prefix webapp", "pip install -r requirements.txt"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT dashboard_app:app"
